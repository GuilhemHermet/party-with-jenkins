pipeline{
    agent any

    stages{
        stage("init"){
            echo "init"
        }
        stage("test"){
            echo "testing"
        }
        stage("build"){
            echo "building"
        }
    }
}